/*
 * WhyPage Messages
 *
 * This contains all the text for the WhyPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WhyPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WhyPage container!',
  },
});
